CAVEMAN MODE.

Fix summarizer CLI chain. Read /tmp/tim-summarizer-cli-journal.md for full diagnosis.

FIXES (in order):

1. **Fix shell injection (Defect A) — `generate-summary.ts` tryCli():**
   - Replace `promisify(exec)(cmd, ...)` with `spawn()` + args array (NO shell)
   - For codex: keep stdin pipe, pass args as array to spawn
   - For opencode: pass prompt via stdin (echo equivalent) or spawn with args array
   - Remove `const q = (s) => JSON.stringify(s)` — no shell quoting needed

2. **Fix maxBuffer (Defect B):**
   - Raise to 512KB or use streaming (collect chunks)

3. **Fix opencode agentic behavior (Defect C):**
   - Check if opencode has a non-agentic flag: `opencode --help 2>&1 | grep -i 'chat\|-c\|completion\|-n\|no-tools\|raw'`
   - If `opencode chat` exists → use that instead of `opencode run`
   - If no flag: accept the latency, keep 600s timeout, but add stdout stderr logging

4. **Fix config — ~/.tim/config.json:**
   - codex `auto` → `gpt-5.4-mini` (tested working for ChatGPT accounts)
   - kimi `openrouter/kimi-k2.6` → `moonshotai/kimi-k2.6`

5. **Capture stderr to summarizer.log:**
   - In tryCli catch block: append stderr to `.tim/summarizer.log`
   - fs.appendFileSync for quick diagnostics

REBUILD: cd packages/tim-summarizer && npm run build
TEST: npx tsc -b --noEmit clean. Then manually run summarizer on ONE session to verify real summaries.

JOURNAL: Write decisions to /tmp/tim-summarizer-fix-journal.md
