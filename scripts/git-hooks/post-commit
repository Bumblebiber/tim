#!/usr/bin/env bash
# Git hook template — symlink into ~/.hermes/git-hooks/post-commit (see post-commit.sh header).
set -euo pipefail
TIM_REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
exec "$TIM_REPO/packages/tim-hooks/scripts/post-commit.sh"
