{
  "name": "tim-store",
  "version": "0.1.0-beta.0",
  "description": "TIM SQLite store: schema, CRUD, search, edges, staging",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "!dist/**/__tests__/**"
  ],
  "scripts": {
    "build": "tsc",
    "pretest": "npm --prefix ../.. run build",
    "test": "vitest run"
  },
  "dependencies": {
    "tim-core": "0.1.0-beta.0",
    "better-sqlite3": "^11.7.0",
    "ulid": "^3.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12"
  }
}
