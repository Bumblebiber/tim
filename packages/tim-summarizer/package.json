{
  "name": "tim-summarizer",
  "version": "0.1.0-beta.1",
  "description": "TIM batch summarizer CLI — MCP client for tim_show_unsummarized / tim_write_batch_summary",
  "main": "dist/summarize.js",
  "bin": {
    "tim-summarizer": "dist/summarize.js"
  },
  "files": [
    "dist/**/*",
    "!dist/**/__tests__/**"
  ],
  "scripts": {
    "build": "tsc",
    "pretest": "npm --prefix ../.. run build",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "tim-core": "0.1.0-beta.1",
    "tim-mcp": "0.1.0-beta.1",
    "tim-store": "0.1.0-beta.1"
  },
  "devDependencies": {
    "tim-hooks": "0.1.0-beta.1"
  }
}
