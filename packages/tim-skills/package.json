{
  "name": "tim-skills",
  "version": "0.1.0-beta.0",
  "description": "TIM agent skills for weak models — using, remember, session-start, explain, hmem import audit",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "skills/**/*",
    "!dist/**/__tests__/**"
  ],
  "scripts": {
    "build": "tsc",
    "pretest": "npm --prefix ../.. run build",
    "test": "vitest run"
  }
}
