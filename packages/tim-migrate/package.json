{
  "name": "tim-migrate",
  "version": "0.1.0-beta.1",
  "description": "TIM migration: hmem → TIM schema converter",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "!dist/**/__tests__/**"
  ],
  "scripts": {
    "build": "tsc",
    "pretest": "npm --prefix ../.. run build",
    "test": "vitest run"
  },
  "dependencies": {
    "tim-core": "0.1.0-beta.1",
    "tim-store": "0.1.0-beta.1",
    "better-sqlite3": "^11.7.0",
    "ulid": "^3.0.0"
  }
}
