{
  "name": "tim-sync-client",
  "version": "0.1.0-alpha",
  "description": "TIM o9k-sync HTTP client: encryption, push/pull cycles",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "!dist/**/__tests__/**"
  ],
  "scripts": {
    "build": "tsc",
    "pretest": "npm --prefix ../.. run build",
    "test": "vitest run"
  },
  "dependencies": {
    "tim-core": "*",
    "tim-store": "*"
  }
}
