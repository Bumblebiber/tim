{
  "name": "tim",
  "version": "0.1.0-alpha",
  "private": true,
  "description": "TIM — Theoretically Infinite Memory. Local-first, self-optimizing cognitive OS for AI agents.",
  "workspaces": [
    "packages/tim-core",
    "packages/tim-store",
    "packages/tim-sync-client",
    "packages/tim-cli",
    "packages/tim-mcp",
    "packages/tim-migrate",
    "packages/tim-hooks",
    "packages/tim-summarizer",
    "packages/tim-skills"
  ],
  "scripts": {
    "prepare": "rm -rf packages/*/tsconfig.tsbuildinfo && npm run build",
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.20.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "fastembed": "^2.1.0"
  }
}
