# TIM Detailed Implementation Plans — Index

> Detailplanung zu `next-steps-rough.md` (Basis: `REVIEW-fable5.md`). Erstellt 2026-07-02 von Fable 5.
> Jeder Plan ist eigenständig ausführbar (superpowers:subagent-driven-development oder executing-plans) und endet mit grünem Test-Suite-Stand + Commits.

| # | Plan | Rough-Block | Abhängigkeit | Umfang |
|---|------|-------------|--------------|--------|
| 1 | [Trust-Killers](2026-07-02-plan-1-trust-killers.md) — suppress enforcement, symmetric update, FTS quoting, sections-by-title | Block 1 | keine | 4 Tasks, klein |
| 2 | [Ops-Safety](2026-07-02-plan-2-ops-safety.md) — transaktionale Migrationen + Auto-Backup, echte Orphan-Metrik | Block 2 | keine | 2 Tasks, klein |
| 3 | [Migrate-Cleanup](2026-07-02-plan-3-migrate-cleanup.md) — legacy migrate.ts löschen, Import sync-fähig (Titel + Staging), Re-Import schreibt Änderungen | Block 3 | keine | 3 Tasks, mittel |
| 4 | [MCP-Surface](2026-07-02-plan-4-mcp-surface.md) — Fehlerkontrakt, bind:false, Tool-Removals, Plumbing-Gate, zod→ListTools-Generierung | Block 4 | keine (Task 1 vor 2–5) | 5 Tasks, groß |
| 5 | [HTTP-Multiclient](2026-07-02-plan-5-http-multiclient.md) — SSE-Server-Leak, per-Client Session-Identität | Block 5 | Plan 4 unabhängig, aber Task 2 fasst dieselben Handler an → nach Plan 4 ausführen | 3 Tasks, mittel |
| 6 | [Sync-Hardening](2026-07-02-plan-6-sync-hardening.md) — Konvergenz-Tests, Tie-Divergenz-Fix (`lww_device`-Spalte), Merkle-Dead-Code löschen | Block 6 | **Plan 2 zuerst** (Migration v8) | 3 Tasks, mittel |
| 7 | [Package-Cleanup](2026-07-02-plan-7-package-cleanup.md) — resolveLWW→tim-core, tim-sync + tim-search löschen, project-output→tim-mcp + Badge-Fix | Block 7 | **Plan 6 zuerst** | 4 Tasks, mittel |

## Empfohlene Reihenfolge

1 → 2 → 3 (unabhängig, beliebig parallelisierbar) → 6 → 7 → 4 → 5.
Begründung: 1–3 sind die Correctness-Fixes; 6+7 räumen auf, bevor 4 die große server.ts-Refaktorierung macht; 5 fasst dieselben Handler wie 4 an und kommt deshalb zuletzt.

## Wichtige Abweichungen vom Rough-Plan

- **Block 6/7:** `tim-sync` wird NICHT in `tim-sync-client` gemergt — `tim-store` importiert `resolveLWW` aus `tim-sync`, und `tim-sync-client` hängt von `tim-store` ab (Merge = Dependency-Zyklus). Stattdessen: `resolveLWW` → `tim-core`, `tim-sync` komplett löschen.
- **Bereits erledigt:** deterministisches LWW (1e8654c) und der `accessed_at`-Timestamp-Bug (sync-methods.ts nutzt `updated_at`) sind auf dem Branch schon gefixt.
- **Neu gefundener Bug (bei der Planung entdeckt):** Timestamp-Ties divergieren trotzdem — der lokale Vergleichsrecord in `applyStaging`/`applyRemoteEntry` nutzt das hartkodierte Device `'local'`, dadurch behält bei identischem Timestamp jedes Replikat seine eigene Version. Fix in Plan 6 Task 2 (`entries.lww_device`-Spalte, Migration v8).
- **Suppress-Enforcement** greift bewusst in `search()`/`loadProject()`/`tim_read` (MCP), nicht in `store.read()` selbst — sonst könnte ein Suppress-Pattern die Summarizer-Pipeline brechen (Begründung in Plan 1).

## Block 8 (Phase-0.7-Features) — bewusst NICHT geplant

Summary-first reads (`include_body`), Tag-TF-IDF/related-on-read und Truncation-Priorität sind Feature-Design, kein Bugfix — sie brauchen zuerst eine superpowers:brainstorming-Session (Fragen: Wie wird "related" gerankt? Wo wird das Summary-Feld gespeichert — neue Spalte = erst Plan 2 mergen? Welche Priorisierungsregeln für Truncation?). Erst nach dem Brainstorming einen Plan schreiben.
